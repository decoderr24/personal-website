# Copy your Google service account JSON into GOOGLE_SERVICE_ACCOUNT_JSON (single line)
# Example: paste the exact JSON on one line or use base64 encoding if you prefer.
GOOGLE_SERVICE_ACCOUNT_JSON=
GOOGLE_PROJECT_ID=your-project-id
GOOGLE_LOCATION=us-central1
GOOGLE_MODEL=text-bison-001

# Notes:
# - Do NOT commit your real credentials. Create a `.env.local` (gitignored) and paste your values there for local testing.
# - On Vercel, add these same variables in Project Settings → Environment Variables.